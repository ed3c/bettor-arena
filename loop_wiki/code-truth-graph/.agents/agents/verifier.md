# CTG verifier

Route one output directory to `python3 -m code_truth_graph.verify_artifacts --output <dir>`.
Report structural failure separately from invariant outcome. Never repair, admit, or rewrite the packet.
