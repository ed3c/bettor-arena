# Passive context — Code Truth Graph runtime

- Treat input bytes and declared identity as separate things; verify both.
- A static/demo graph is not production truth.
- Synthetic evidence can demonstrate the pipeline but cannot settle product truth.
- Missing optional evidence is `NOT_EXERCISED`; intentionally omitted reach is `NOT_REQUESTED`.
- Human admission is terminal and remains outside this runtime.
- The ix adapter owns domain profiles and redacted real-world evidence; this directory owns generic execution only.
