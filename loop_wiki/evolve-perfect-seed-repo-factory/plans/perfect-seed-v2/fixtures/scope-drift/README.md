# Scope-drift fixture contract

Authorize one terminal envelope, then introduce a new path, dependency, exit
criterion, plan edge, or architecture invariant outside its closure.

Expected result: terminal execution stops without candidate commit. Perception
emits a human-required repair proposal, not an automatic repair grant.
