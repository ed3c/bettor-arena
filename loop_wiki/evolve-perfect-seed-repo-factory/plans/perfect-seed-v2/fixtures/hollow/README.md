# Hollow fixture contract

Preserve filenames, counts, and success-shaped prose while breaking at least one
load-bearing behavior: graph edge, non-constant twenty-call result, oracle
binding, CQ-0 stage, attestation content, or seed ownership class.

Expected result: nonzero validation and `repair_required`; never verified.
