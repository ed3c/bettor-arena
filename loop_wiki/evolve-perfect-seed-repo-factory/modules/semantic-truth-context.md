# Fixed semantic-truth context

The human wants a repo that preserves enough context for a fresh agent to make
non-fuzzy decisions. Keep source, evidence, claims, unknowns, decisions,
capabilities, call dependencies, outputs, and lineage separate. Never compress
them into one prompt or one “done” label. Model or script output is evidence;
human admission remains separate.
