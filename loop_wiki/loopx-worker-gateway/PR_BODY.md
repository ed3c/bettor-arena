# PR body source

Closes #64.

The leaf adds a host-neutral six-adapter Worker protocol and fixture-proven, host-owned gateway. It does not merge, select the module into the shared composition, expose it through MCP, activate credentials/providers, claim physical isolation, run live host canaries, write LoopX state, decide Gates or perform Human Admit.
