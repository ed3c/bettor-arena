# No live claims

The following are insufficient to change any host from `NOT_EXERCISED`:

```text
adapter JSON exists
binary name is documented
source repository is visible
Skill discovery path is known
fixture gateway passes
another host passes
old receipt exists
```

A live claim requires an exact current host/runtime/provider/session subject and redacted execution receipt, followed by independent Gates and Human admission where applicable.
