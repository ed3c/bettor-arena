# Terminal issue binding

Owner: `ed3c/bettor-arena#64` — LoopX Worker Gateway v1.

This leaf is a sibling of the append-only ledger leaf and is based on `feat/loopx-contract-v1`. It owns the six-host adapter protocol and fixture-proven gateway mechanism only. Shared composition/locks, live host canaries, runtime-fabric isolation and final acceptance remain outside this leaf.
