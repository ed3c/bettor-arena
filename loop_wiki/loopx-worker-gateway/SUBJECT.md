# Exact-subject rule

Every request and receipt binds:

```text
repository identity
40-character commit
40-character tree
task ID
adapter manifest digest
Skill content/projection digests
Context Capsule/instruction digests
workspace lease ID
```

Any mismatch is a checked refusal. No mutable branch name, local path, installed binary, old receipt or another host can fill a missing subject field.
