# Operator-only actions

Only a trusted Human/operator may merge, select this module into the shared composition, widen network/credential permissions, admit a live adapter, sign a scoped exception, promote or roll back a release.
