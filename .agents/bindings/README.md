# `.agents/bindings/`

Owner: resolved immutable Skill closure for this consumer. Bindings pin source identity and selected Skill bytes/digests. They are generated/resolved state, not hand-written provider evidence.

A binding that is internally self-consistent but points to an unreachable source cannot prove remote reproducibility; origin reachability/equivalence is a separate gate.
