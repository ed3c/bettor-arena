# CLAUDE.md — bettor-arena(Claude-tier 薄派生)

工程 SSOT＝`ARCHITECTURE.md`(§2 放置契約、§3 鐵律),本檔只給入口,禁複述。

- 新檔先對映 §2 槽位;無槽位先改圖再落檔。
- T0:`python3 scripts/gates/check_root_coupling.py`(tracked 檔禁絕對家目錄路徑)。
- 啟用:`sh bootstrap.sh`(冪等;相對 hooksPath+doctor)。
- 迭代期間 root 被動上下文凍結(cache 前綴正確性,非風格偏好)。
