name: Code Truth Graph v2

on:
  push:
    branches:
      - 'feat/loopx-code-truth-graph-v2'
    paths:
      - 'loop_wiki/code-truth-graph-v2/**'
      - '.arena/modules/code-truth-graph-v2/**'
      - '.github/workflows/code-truth-graph-v2.yml'
  pull_request:
    # ready_for_review has to be listed explicitly: the default type set is
    # opened/synchronize/reopened, so with the draft guard below a PR that
    # opens as a draft skips this job and never re-runs when it is marked
    # ready -- leaving the module verified only by a branch push, against
    # whatever commit that was rather than the head under review.
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - 'loop_wiki/code-truth-graph-v2/**'
      - '.arena/modules/code-truth-graph-v2/**'
      - '.github/workflows/code-truth-graph-v2.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: code-truth-graph-v2-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contracts:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
      - name: Validate evidence planes, Python AST reference, coverage and planted controls
        run: sh loop_wiki/code-truth-graph-v2/tests/run-all.sh
      - name: Validate module catalog without composition selection
        run: python3 scripts/arena_modules.py catalog >/dev/null
