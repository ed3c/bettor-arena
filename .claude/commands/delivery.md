---
description: forgejo-delivery-loop 的相容別名；程序只存在 canonical skill
argument-hint: "[arguments]"
---

載入並完整遵循 `.claude/skills/forgejo-delivery-loop/SKILL.md`，將 `$ARGUMENTS` 原樣交給它處理。
本檔不得自行定義分派、worktree、Forgejo 或驗證程序。
