# Controlled-language consumer receipts

This directory reserves the consumer evidence route. CTL 07A creates no
physical carrier, provider, confidential-processing, terminology-admission,
memory-writeback, or official-compliance receipt.

```text
offline binding mechanism       IMPLEMENTED
exact-head hosted contract      NOT_EXERCISED until its workflow runs
Codex physical matrix           NOT_EXERCISED
Claude physical matrix          NOT_EXERCISED
production termbase             ABSENT
external confidential lane      NOT_EXERCISED
mem0 / CONTEXT writeback        NOT_IMPLEMENTED
official compliance             NOT_CLAIMED
```

A later physical receipt must bind the consumer commit/tree, immutable upstream
bundle, carrier and harness version, model/environment, condition/repetition,
input/output digests, deterministic verifier, privacy lane, termbase identity,
and any required Human approval. Do not store credentials, document contents,
private sessions, private reasoning, or mutable branch names as authority.
