<results>
  <result id="Q-08" status="PARTIAL">
    <missing>/openwiki/host-loop/mcp-surface.md lacks: the `_equivalent` helper's role in `build_plan` drift detection; the explicit `previous_receipt_sha256` field written by `write_receipt` and `check_receipt_chain`'s tamper-rejection behavior (chain linking is only described generically via the README/check-receipts command); and citations of the focused tests `test_migrate.py:test_receipt_chain_rejects_tampering` and `test_skipping_declared_probes_is_not_a_pass` (only the restore-on-failure and probe-mutation tests are cited).</missing>
  </result>
  <result id="Q-09" status="PARTIAL">
    <missing>/openwiki/host-loop/mcp-surface.md lacks: the `server.py` stdio surface mechanics, including `REPO_CONTEXT_ROOT` resolution (only the two tool names are listed); the rationale for why signatures and `unresolved_dynamic_call` are mandatory evidence; deep-copy on cache hit (only deep-copy on store is documented); and citations of focused tests `test_engine.py:test_rejects_absolute_traversal_and_symlink_escape`, `test_mandatory_signature_is_never_silently_truncated`, `test_budget_retains_mandatory_facts_and_reports_truncation`, and the `test_server.py` stdio-surface tests (only the benchmark-receipt test is named).</missing>
  </result>
</results>
