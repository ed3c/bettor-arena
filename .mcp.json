{
  "mcpServers": {
    "grepai": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "repo_root=\"$(git rev-parse --show-toplevel)\" || exit 1; cd \"$repo_root\" || exit 1; exec grepai mcp-serve"
      ],
      "env": {}
    },
    "repo-context-pack": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "repo_root=\"$(git rev-parse --show-toplevel)\" || exit 1; cd \"$repo_root\" || exit 1; exec uv run --project mcp/context-pack --frozen repo-context-pack-mcp"
      ],
      "env": {}
    },
    "serena": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "repo_root=\"$(git rev-parse --show-toplevel)\" || exit 1; cd \"$repo_root\" || exit 1; exec uvx --from git+https://github.com/oraios/serena@32308a871c13f8fa5936c7d44a8bca710001277e serena start-mcp-server --context claude-code --project-from-cwd --enable-web-dashboard false --open-web-dashboard false --tool-timeout 60"
      ],
      "env": {}
    }
  }
}
